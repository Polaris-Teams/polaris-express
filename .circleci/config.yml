jobs:
  test:
    executor:
      name: node/default
      tag: '14'
    steps:
      - checkout
      - node/install-packages
      - run:
          command: npm run pretest
orbs:
  node: 'circleci/node:x.y'
version: 2.1
workflows:
  test_my_app:
    jobs:
      - test